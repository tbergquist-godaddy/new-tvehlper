/** @type {import("prettier").Config} */
const config = {
  bracketSpacing: true,
  printWidth: 100,
  singleQuote: true,
  tabWidth: 2,
  trailingComma: 'all',
  quoteProps: 'consistent',
};

module.exports = config;
